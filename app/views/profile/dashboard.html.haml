%p#notice= notice
%h1
  Hello #{current_user.name}. Thank you for visit Olimpia bank

%h2.move_block List of your account
%table
  %thead
    %tr
      %th Balance
      %th Currency
      %th{:colspan => "3"}
  %tbody
    - @accounts.each do |account|
      %tr
        %td= account.balance
        %td= account.currency
        %td= link_to 'Deposite', deposite_profile_account_path(account)
        %td= link_to 'Withdraw', withdraw_profile_account_path(account)
        %td= link_to 'Destroy', account, method: :delete, data: { confirm: 'Are you sure?' }
%br/
= link_to 'New Account', new_account_path
